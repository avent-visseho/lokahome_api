# Upstream API
upstream lokahome_api {
    server api:8000;
}

# Redirect HTTP to HTTPS (activé après SSL)
# server {
#     listen 80;
#     server_name api.lokahome.bj;
#     return 301 https://$server_name$request_uri;
# }

# HTTP Server (initial - avant SSL)
server {
    listen 80;
    server_name _;

    # Let's Encrypt challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # API Proxy
    location / {
        proxy_pass http://lokahome_api;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 86400;
    }

    # Static files
    location /static/ {
        alias /app/static/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Rate limiting on auth endpoints
    location /api/v1/auth/ {
        limit_req zone=auth burst=5 nodelay;
        proxy_pass http://lokahome_api;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Health check (pas de rate limit)
    location /health {
        proxy_pass http://lokahome_api;
        access_log off;
    }
}

# HTTPS Server (décommenter après obtention du certificat SSL)
# server {
#     listen 443 ssl http2;
#     server_name api.lokahome.bj;
#
#     ssl_certificate /etc/letsencrypt/live/api.lokahome.bj/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.lokahome.bj/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#
#     location / {
#         proxy_pass http://lokahome_api;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_read_timeout 86400;
#     }
#
#     location /static/ {
#         alias /app/static/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location /api/v1/auth/ {
#         limit_req zone=auth burst=5 nodelay;
#         proxy_pass http://lokahome_api;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location /health {
#         proxy_pass http://lokahome_api;
#         access_log off;
#     }
# }
